# LISTA BIANCA: tutto escluso, si pubblica solo il minimo per far girare la previsione.
# Restano fuori (e quindi privati): script di addestramento, benchmark, validazione,
# dati storici grezzi, il modulo vento v21 e l'archivio.
*

!.gitignore
!README.md

# Codice di inferenza
!previsione_onda.py
!onda_features.py
!vento_features.py
# Design (palette, CSS, sfondo, grafico): l'unico file da toccare per un redesign
!stile.py

# Modelli addestrati
!modello_onda.pkl
!modello_vento.pkl

# Ambiente + automazione
!requirements.txt
# !CNAME  <- riattivare SOLO dopo aver configurato il DNS (vedi workflow)
!.github/
!.github/workflows/
!.github/workflows/dashboard.yml

# Archivio previsioni: la meta' "promesse" della pagella reale (il workflow
# lo ricommitta a ogni run)
!archivio_previsioni.csv
